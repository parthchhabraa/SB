# Point these at your Supabase instance (hosted, or self-hosted on your own server).
NEXT_PUBLIC_SUPABASE_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY=replace-me

# Only used by scripts/dev-db.sh for the throwaway local test cluster.
# Never point this at a database you care about.
TEST_DATABASE_URL=postgresql://localhost:55432/study_tracker_test
